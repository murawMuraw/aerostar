// core/World.js

class World {

    constructor(game) {

        this.game = game;

        this.time = 0;

        this.tickCount = 0;

    }

    //----------------------------------------------------------
    // Запуск мира
    //----------------------------------------------------------

    start() {

        console.log("World started");

    }

    //----------------------------------------------------------
    // Остановка
    //----------------------------------------------------------

    stop() {

        console.log("World stopped");

    }

    //----------------------------------------------------------
    // Один игровой тик
    //----------------------------------------------------------

    update(dt) {

        this.time += dt;
        this.tickCount++;

        //------------------------------------------------------
        // Ветер
        //------------------------------------------------------

        if (this.game.wind.update)
            this.game.wind.update(dt);

        //------------------------------------------------------
        // Течение
        //------------------------------------------------------

        if (this.game.ocean.update)
            this.game.ocean.update(dt);

        //------------------------------------------------------
        // Движение кораблей
        //------------------------------------------------------

        this.updateShips(dt);

        //------------------------------------------------------
        // Проверка регаты
        //------------------------------------------------------

        if (this.game.race.update)
            this.game.race.update(dt);

    }

    //----------------------------------------------------------
    // Обновление кораблей
    //----------------------------------------------------------

    updateShips(dt) {

        const ships = this.game.ships.getActive();

        for (const ship of ships) {

            if (ship.anchor)
                continue;

            //----------------------------------------------
            // Здесь позже появится физика движения
            //----------------------------------------------

        }

    }

    //----------------------------------------------------------
    // Полное состояние мира
    //----------------------------------------------------------

    getState() {

        return {

            time: this.time,

            tick: this.tickCount,

            ships: this.game.ships.getActive(),

            players: this.game.players.getOnline()

        };

    }

}

module.exports = World;
